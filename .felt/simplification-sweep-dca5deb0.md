---
title: Simplification sweep
status: open
tags:
    - '[portolan]'
created-at: 2026-03-06T10:46:31.715433+01:00
---

Deep simplification of the portolan codebase. Not a checklist — a desired state. Survey reality, find the largest gap, close it.

## Desired State

Every source file owns one concern. You can describe what a file does in one sentence without conjunctions. No file exceeds 800 LOC — not because of an arbitrary limit, but because a single concern rarely needs more than that. When a file has section headers (`// === X ===`), those sections have become their own modules.

Dead code is gone. Redundant abstractions are collapsed. Names say what they mean. The codebase does exactly what it does now, with fewer lines and clearer structure.

## Scope

Everything is in scope: extracting modules, splitting classes, moving functions, consolidating duplication, renaming, removing dead code. Observable behavior must not change.

## Principles

**Do the hard work.** The small files are already clean. What remains is structural — understanding how large files decompose along natural seams. This requires reading, mapping internal structure, and making judgment calls about what belongs together.

**"Well-organized despite its size" is not clean.** Section headers inside a file are evidence that the file contains multiple concerns. They are the seams along which to extract.

**Run /simplify, don't just read.** Invoke the skill and let it analyze. Your judgment plus /simplify's analysis is stronger than either alone.

**One concern per iteration.** Don't try to simplify the whole codebase in one pass. Pick the file with the most to gain, understand it deeply, restructure it, verify, commit. Then survey again with fresh eyes.

## Method

Each iteration:

1. **Survey.** Find source files that violate the desired state. `find src server/src -name '*.ts' -o -name '*.js' | xargs wc -l | sort -rn` shows the largest. Read section headers to understand internal structure. Pick the one with the most to gain.
2. **Read completely.** Map the file's concerns — what are the distinct responsibilities? What would you name each section if it were its own module?
3. **Invoke `/simplify`** on the file. Follow its analysis.
4. **Extract.** Create new modules for distinct concerns. Update all imports across the codebase (`grep -r 'from.*oldfile' src/ server/src/`). The original file should become a thin coordinator or disappear entirely.
5. **Verify.** `cd server && npm test` for server changes. `npm run build` for frontend. Both must pass.
6. **Commit** with `simplify: <what you did>`.
7. **Update this fiber** — add a brief note to the log below describing what you found and what you did. This is how the next iteration orients.

## Evidence

```bash
find src server/src -name '*.ts' -o -name '*.js' | xargs wc -l | sort -rn | head -20
cd server && npm test
npm run build
```

When no file exceeds ~800 LOC and the largest files each own a single describable concern, the sweep is done.

## Context

The codebase is ~21k LOC across ~40 source files. Files under 500 LOC have been reviewed and are clean. The structural work is in the large files. Key areas of complexity:

- **Server:** `HttpApi.ts` has 49 methods across 13+ endpoints with section headers. `index.ts` mixes WS dispatch, search, and directory browsing.
- **Frontend UI:** `TapestryView.ts` (3.2k) combines D3 force simulation, fiber detail editing, claims sidebar, and wave reveal animation. `FileViewerModal.ts` (2.3k) combines CodeMirror, PDF rendering, image lightbox, and annotation toolbar.
- **Render:** `ZoneRenderer.ts` (1.4k) handles hex meshes, city sprites, labels, drag state, and tooltips.
- **Entry:** `main.ts` (1.3k) wires Three.js setup, WebSocket connection, render loop, and event dispatch.

## Log

**Iteration 4 (2026-03-06):** Surveyed all files. TapestryView.ts (3226 LOC) is the largest by far with 13 section headers. Extracted types to `tapestry-types.ts` (91 LOC) and pure helper functions (geometry, staleness, text formatting, neighbor analysis) to `tapestry-helpers.ts` (160 LOC). TapestryView.ts now 2987 LOC. Next targets: the class still has ~10 distinct concerns behind section headers. The biggest remaining extraction opportunities are annotations (~300 LOC), lightbox (~110 LOC), inline markdown editing (~165 LOC), and the static file modal. FileViewerModal.ts (2287) and HttpApi.ts (2004) are the next-largest files.

**Iteration 5 (2026-03-06):** Stayed on `TapestryView.ts` and extracted two embedded UI subsystems: the static export file viewer to `TapestryStaticFileModal.ts` (167 LOC) and artifact lightbox navigation/annotation to `TapestryArtifactLightbox.ts` (146 LOC). `TapestryView.ts` dropped to 2743 LOC and now delegates those concerns instead of owning their DOM/event lifecycles inline. Verified with `npm run build`. Next seams in the file are the inline markdown editor and the claims annotation workflow; after that, `FileViewerModal.ts` and `HttpApi.ts` remain the largest structural targets.

**Iteration 6 (2026-03-06):** Stayed on `TapestryView.ts` because the inline markdown body editor was still a self-contained subsystem embedded in the class. Extracted rendered-body and CodeMirror edit/save lifecycle into `TapestryDetailBody.ts` (166 LOC), including markdown rendering, inline path wiring, dirty tracking, and fiber file persistence. `TapestryView.ts` dropped to 2619 LOC and now coordinates body editing instead of owning editor construction and save logic directly. Verified with `npm run build`. Next seams are the claims annotation workflow inside `TapestryView.ts`, then `FileViewerModal.ts` and `HttpApi.ts`.

**Iteration 7 (2026-03-10):** Stayed on `TapestryView.ts` and extracted the claims annotation subsystem to `TapestryClaimsAnnotations.ts` (379 LOC). The new module now owns annotation panel wiring, popover UI, claims persistence, worker handoff, and file-as-fiber actions; `TapestryView.ts` dropped to 2276 LOC and now only forwards selection and image-pin events into that controller. Verified with `npm run build`. Next seams in `TapestryView.ts` are the remaining detail/sidebar coordination and DAG interaction logic; after that, `FileViewerModal.ts` and `HttpApi.ts` remain the largest structural targets.

**Iteration 8 (2026-03-10):** Shifted to `FileViewerModal.ts`, which was still mixing modal shell, file viewing, editor lifecycle, rendered-markdown selection, image pinning, annotation persistence, worker dispatch, and file-as-fiber actions. Extracted the annotation application to `FileViewerAnnotations.ts` (799 LOC), including CodeMirror highlight state, annotation panel wiring, text selection toolbars, image annotation popovers, worker handoff, and filing feedback as fibers. `FileViewerModal.ts` dropped from 2287 LOC to 1421 LOC and now focuses on loading files plus coordinating editor/rendered/PDF/image presentation. Verified with `npm run build`. Next structural seams are the remaining rendered-markdown/fiber rendering logic inside `FileViewerModal.ts`, then `HttpApi.ts`, `server/src/index.ts`, `ZoneRenderer.ts`, and `main.ts`.

**Iteration 9 (2026-03-10):** Stayed on `FileViewerModal.ts` because the rendered-markdown path was still a separate UI subsystem embedded inside the modal. Extracted markdown DOM creation, fiber frontmatter/card rendering, inline path navigation, rendered selection handling, and cancelable tapestry context hydration into `FileViewerMarkdownView.ts` (361 LOC). `FileViewerModal.ts` dropped from 1421 LOC to 1111 LOC and now focuses on modal shell, file loading, editor/image/PDF presentation, and save/refresh/copy/download actions. Verified with `npm run build`. Next structural seams are the remaining editor/file-loading coordination inside `FileViewerModal.ts`, then `HttpApi.ts`, `server/src/index.ts`, `ZoneRenderer.ts`, and `main.ts`.

**Iteration 10 (2026-03-10):** Stayed on `FileViewerModal.ts` because the remaining editable-text workspace was still a distinct subsystem embedded inside the modal shell. Extracted CodeMirror setup, dirty tracking, markdown edit-mode transitions, save/copy/download actions, and text-file state into `FileViewerTextEditor.ts` (429 LOC). `FileViewerModal.ts` dropped from 1111 LOC to 739 LOC and now coordinates request ownership, modal chrome, navigation keys, and image/PDF/text presentation instead of owning the editor internals. Verified with `npm run build`. Next structural seams are the file-loading/request coordination still inside `FileViewerModal.ts`, then `HttpApi.ts`, `server/src/index.ts`, `ZoneRenderer.ts`, and `main.ts`.

**Iteration 11 (2026-03-10):** Shifted to `HttpApi.ts`, which was still mixing route dispatch with the full annotations workflow. Extracted annotation CRUD, recent-annotation queries, worker send/paste formatting, file-as-fiber filing, and felt promotion into `HttpApiAnnotations.ts` (485 LOC). `HttpApi.ts` dropped from 2004 LOC to 1524 LOC and now routes those endpoints into a dedicated controller while keeping file content, tapestry, playground, and hook/session concerns separate. Verified with `cd server && npm test` and `npm run build`. Next structural seams are the remaining endpoint families inside `HttpApi.ts`, then `server/src/index.ts`, `ZoneRenderer.ts`, and `main.ts`.

**Iteration 12 (2026-03-10):** Stayed on `HttpApi.ts` because the file-content subsystem was still a separate concern embedded alongside route dispatch, tapestry building, activation, playground serving, and hook/session diagnostics. Extracted file loading, binary/raw streaming, save-file persistence, MIME mapping, SSH read/write helpers, and language detection into `HttpApiFileContent.ts` (541 LOC). `HttpApi.ts` dropped from 1524 LOC to 986 LOC and now coordinates routing plus tapestry/activation/playground/hook/runtime concerns instead of owning editor file I/O internals. Verified with `cd server && npm test` and `npm run build`. Next structural seams are the remaining endpoint families inside `HttpApi.ts` (especially tapestry vs playground/activation/hook/runtime coordination), then `server/src/index.ts`, `ZoneRenderer.ts`, and `main.ts`.

**Iteration 13 (2026-03-10):** Shifted to `server/src/index.ts`, which was still mixing websocket/server coordination with the full workspace-browsing subsystem. Extracted local/remote file search, active-search cancellation, git-aware directory listing, and SSH-backed remote directory reads into `WorkspaceBrowser.ts` (428 LOC). `server/src/index.ts` dropped from 1494 LOC to 1050 LOC and now focuses more tightly on state coordination, remote session handling, websocket wiring, and startup/shutdown. Verified with `cd server && npm test` and `npm run build`. Next structural seams are the remaining websocket concerns inside `server/src/index.ts`, then `ZoneRenderer.ts`, `main.ts`, and the still-large coordination logic in `TapestryView.ts`.

**Iteration 14 (2026-03-10):** Stayed on `server/src/index.ts` because the entrypoint still owned the remote-agent websocket state machine: remote session maps, activity persistence, git status tracking, working-timeout cleanup, and agent message handling. Extracted that subsystem to `RemoteAgentCoordinator.ts` (377 LOC), including remote session lifecycle, activity dedup/persistence, git status bookkeeping, working-timeout expiry, and SSH tunnel reconnects. `server/src/index.ts` dropped from 1050 LOC to 741 LOC and now coordinates state assembly plus HTTP/WebSocket startup instead of owning remote agent internals. Verified with `cd server && npm test` and `npm run build`. Next structural seams are `ZoneRenderer.ts`, `main.ts`, and the still-large coordination logic in `TapestryView.ts`.

**Iteration 15 (2026-03-10):** Shifted to `main.ts`, which was still mixing bootstrap with browser input handling, debug-runtime instrumentation, and dead view-switching code. Extracted canvas/document/window event handling into `MapInteractionController.ts` (361 LOC), moved debug runtime surface and diagnostics snapshots into `FrontendRuntimeDiagnostics.ts` (183 LOC), and pulled city lookup helpers into `cityLookup.ts` (35 LOC) while removing the unused view-overlay/plans-switching path. `main.ts` dropped from 1316 LOC to 798 LOC and now coordinates renderer/UI setup, websocket state, and lifecycle instead of owning the full interaction lattice inline. Verified with `npm run build`. Next structural seams are `ZoneRenderer.ts` and the still-large coordination logic in `TapestryView.ts`; after that, the remaining oversize production file is `HttpApi.ts` at 986 LOC.

**Iteration 16 (2026-03-10):** Shifted to `ZoneRenderer.ts`, which was still mixing scene rendering with the worker recent-files tooltip UI. Extracted tooltip DOM ownership, hover/hide timers, fetch lifecycle, positioning, and click routing into `ZoneRendererWorkerTooltip.ts` (278 LOC). `ZoneRenderer.ts` dropped from 1413 LOC to 1178 LOC and now keeps render state plus a narrow delegation surface for tooltip hover/click APIs instead of owning the tooltip subsystem inline. Verified with `npm run build`. Next structural seams inside `ZoneRenderer.ts` are the label-drag interaction controller and the city/worker render construction paths; after that, `TapestryView.ts` and `HttpApi.ts` remain the largest oversize production files.

**Iteration 17 (2026-03-10):** Stayed on `ZoneRenderer.ts` because the label interaction state machine was still embedded inside rendering: worker/city label click wiring, hover callbacks, screen-to-world drag conversion, drag listener lifecycle, and click suppression after drags. Extracted that subsystem to `ZoneRendererLabelInteractions.ts` (242 LOC). `ZoneRenderer.ts` dropped from 1178 LOC to 976 LOC and now focuses on scene construction, render diffing, animation, and tooltip coordination instead of owning DOM label interaction mechanics. Verified with `npm run build`. Next structural seams are the city/worker render construction paths inside `ZoneRenderer.ts`; after that, `TapestryView.ts` and `HttpApi.ts` remain the largest oversize production files.

**Iteration 18 (2026-03-10):** Stayed on `ZoneRenderer.ts` because entity construction and render-time hit testing were still embedded inside the renderer alongside scene coordination. Extracted city sprite fallback meshes, worker/orphan swarm assembly, label DOM creation, hex-grid overlays, activity decals, hit tests, and swarm disposal into `ZoneRendererEntities.ts` (536 LOC). `ZoneRenderer.ts` dropped from 976 LOC to 463 LOC and now coordinates ground plane/rhumb lines, state diffing, animation policy, tooltip delegation, and diagnostics instead of owning the full city/worker render subsystem. Verified with `npm run build`. Next structural targets are `HttpApi.ts` at 986 LOC and the still-large coordination logic in `TapestryView.ts`.

**Iteration 19 (2026-03-10):** Shifted back to `TapestryView.ts`, which was still mixing DAG coordination with the full sidebar/detail presentation subsystem. Extracted detail rendering, artifact-lightbox click wiring, resize handling, node/fiber detail presentation, and body-edit action swapping into `TapestryDetailPanel.ts` (455 LOC). `TapestryView.ts` dropped from 2276 LOC to 1908 LOC and now focuses more tightly on DAG layout, reveal/highlight behavior, selection state, search, and annotation coordination instead of owning sidebar/detail DOM lifecycle inline. Verified with `npm run build`. Next structural seams in `TapestryView.ts` are the remaining DAG interaction/reveal/search coordination; after that, `HttpApi.ts` at 986 LOC is still the only oversize server production file.

**Iteration 20 (2026-03-10):** Stayed on `TapestryView.ts` because the remaining D3 canvas runtime was still embedded inside the coordinator alongside data loading, search, detail wiring, and annotation handoff. Extracted graph rendering, force/zoom/drag lifecycle, hover tooltip behavior, visibility/reveal state, and node selection/highlighting into `TapestryDagGraph.ts`, then split node ornamentation and edge-path geometry into `TapestryDagRendering.ts` so the controller stayed focused on orchestration. `TapestryView.ts` dropped from 1908 LOC to 815 LOC and now coordinates data fetching, sidebar/search, detail presentation, and annotation wiring instead of owning the full DAG runtime inline. Verified with `npm run build`. Next seams are the remaining graph/layout split needed to get `TapestryDagGraph.ts` under the target ceiling, then `HttpApi.ts` at 986 LOC.

**Iteration 21 (2026-03-10):** Shifted back to `HttpApi.ts`, which was still mixing route dispatch with the full tapestry DAG/evidence/config/asset-serving subsystem. Extracted that family into `HttpApiTapestry.ts` (328 LOC), including local/remote fiber reads, evidence batching, config flattening, and `/tapestry-asset` binary streaming through the existing file-content helpers. `HttpApi.ts` dropped from 986 LOC to 682 LOC and now focuses on endpoint routing plus activation/playground/hook/runtime coordination instead of owning tapestry assembly inline. Verified with `cd server && npm test` and `npm run build`. Next structural targets are `TapestryDagGraph.ts` at 819 LOC, `TapestryView.ts` at 815 LOC, and `CityHUD.ts` at 896 LOC.

**Iteration 22 (2026-03-11):** Shifted to `CityHUD.ts`, which was still mixing HUD shell coordination with the full directory-browser subsystem. Extracted directory cache state, websocket `listDirectory` requests, expansion/loading/error bookkeeping, row click handling, and recursive tree rendering into `CityHUDFileTree.ts` (224 LOC). `CityHUD.ts` dropped from 896 LOC to 721 LOC and now focuses on tabs, search, fibers, worker chips, and modal visibility instead of owning file-browser internals. Verified with `npm run build` and `cd server && npm test`. Next structural targets are `TapestryDagGraph.ts` at 819 LOC and `TapestryView.ts` at 815 LOC; after that, `src/ui/utils.ts` at 801 LOC should be checked to confirm whether it still owns one concern or needs a split.

**Iteration 23 (2026-03-11):** Stayed on `TapestryDagGraph.ts` because the graph controller was still mixing runtime orchestration with the full DAG layout-construction pipeline. Extracted depth/section anchoring, force-simulation bootstrapping, and viewport-centering into `TapestryDagLayout.ts` (165 LOC). `TapestryDagGraph.ts` dropped from 819 LOC to 688 LOC and now focuses on SVG/runtime interaction, visibility/reveal behavior, tooltip handling, and node highlighting instead of owning initial layout synthesis inline. Verified with `npm run build`. Next structural targets are `TapestryView.ts` at 815 LOC and `src/ui/utils.ts` at 801 LOC; after that, all remaining production files are under the target ceiling.

**Iteration 24 (2026-03-11):** Surveyed `src/ui/utils.ts` after it hit 801 LOC and confirmed it was still mixing a distinct artifact-media subsystem with general markdown/UI helpers. Extracted PDF.js loading, artifact gallery rendering, PDF/image warm caches, and cache diagnostics into `ArtifactMedia.ts` (488 LOC); `utils.ts` dropped to 298 LOC and now focuses on markdown rendering, inline-path wiring, formatting helpers, and toast UI. Verified with `npm run build` and `cd server && npm test`. Next structural target is `TapestryView.ts` at 815 LOC; after that, the remaining production files are under the target ceiling and should be checked for single-concern clarity rather than size alone.

**Iteration 25 (2026-03-11):** Stayed on `TapestryView.ts` because the file was still over the target and still mixed view orchestration with the entire sidebar search/list subsystem. Extracted search input events, keyboard focus, node-match highlighting, fiber filtering/sorting, and click dispatch into `TapestrySidebar.ts` (215 LOC). `TapestryView.ts` dropped from 815 LOC to 670 LOC and now coordinates data fetches, DAG selection, detail rendering, artifact preloading, and annotation handoff instead of owning sidebar UI state inline. Verified with `npm run build` and `cd server && npm test`. Next step is to survey the remaining production files under the size ceiling for single-concern clarity, starting with `main.ts`, `server/src/index.ts`, `FileViewerAnnotations.ts`, and `FileViewerModal.ts`.

**Iteration 26 (2026-03-11):** Surveyed the remaining sub-800 production files and found that `main.ts` still mixed application bootstrap with the full browser-server state-sync runtime. Extracted WebSocket connection lifecycle, server-message routing, reconnect state, activity buffering, and diagnostics accessors into `FrontendStateSync.ts` (332 LOC). `main.ts` dropped from 798 LOC to 521 LOC and now focuses on renderer/UI wiring, map actions, initial camera focus, and lifecycle coordination. Verified with `npm run build` and `cd server && npm test`. Next step is to continue the single-concern survey on `server/src/index.ts`, `FileViewerAnnotations.ts`, and `FileViewerModal.ts`, which are all under the size ceiling but still the likeliest files to contain embedded subsystems.

**Iteration 27 (2026-03-11):** Stayed on `FileViewerAnnotations.ts` because it still mixed the text-selection annotation workflow with a separate image-point annotation UI subsystem. Extracted marker rendering, click-to-annotate popover lifecycle, and outside-click cleanup into `FileViewerImageAnnotations.ts` (173 LOC); `FileViewerAnnotations.ts` dropped from 799 LOC to 684 LOC and now focuses on CodeMirror highlight state, text annotation persistence, worker handoff, and file-as-fiber actions instead of owning mixed media UI behavior inline. Verified with `npm run build` and `cd server && npm test`. Next step is to continue the single-concern survey on `server/src/index.ts` and `FileViewerModal.ts`; after that, review whether `FileViewerTextEditor.ts` or `FileViewerMarkdownView.ts` still hide secondary subsystems despite being comfortably under the size ceiling.

**Iteration 28 (2026-03-11):** Stayed on `FileViewerModal.ts` because it still mixed modal shell coordination with the file-loading and media/text presentation runtime. Extracted request ownership, current file identity, raw asset URL building, image/PDF rendering, and text-file loading/annotation hydration into `FileViewerContentPresenter.ts` (353 LOC); `FileViewerModal.ts` dropped from 733 LOC to 496 LOC and now focuses on modal chrome, navigation keys, deferred UI cleanup, and close semantics instead of owning file presentation internals. Verified with `npm run build` and `cd server && npm test`. Next step is to continue the single-concern survey on `server/src/index.ts`, then check whether `FileViewerMarkdownView.ts` still hides the fiber-context hydration subsystem despite being under the size ceiling.

**Iteration 29 (2026-03-11):** Stayed on `server/src/index.ts` because it still mixed server bootstrap with the full browser-state runtime: state assembly, local-session reconciliation, browser client tracking, fiber refresh polling, and city/fiber websocket mutations. Extracted that subsystem to `BrowserStateCoordinator.ts` (435 LOC), including local/remote fiber reads plus city pin/unpin/move handlers. `server/src/index.ts` dropped from 741 LOC to 383 LOC and now focuses on manager setup, HTTP/WebSocket wiring, agent message routing, and startup/shutdown instead of owning browser-state coordination inline. Verified with `cd server && npm test` and `npm run build`. Next step is to continue the single-concern survey on `FileViewerMarkdownView.ts`, `FileViewerTextEditor.ts`, and the remaining server/frontend coordinators that are still under the size ceiling but may hide secondary subsystems.

**Iteration 30 (2026-03-11):** Stayed on `FileViewerMarkdownView.ts` because it still mixed rendered-markdown interaction with the full felt-fiber parsing and tapestry-context hydration runtime. Extracted fiber frontmatter parsing, fiber-card header rendering, cancelable tapestry fetch lifecycle, staleness/downstream decoration, and evidence gallery/metrics hydration into `FileViewerFiberContext.ts` (274 LOC); `FileViewerMarkdownView.ts` dropped from 361 LOC to 135 LOC and now focuses on rendered markdown display, inline-path navigation, edit-mode entry, and selection forwarding instead of owning felt-specific context logic inline. Verified with `npm run build` and `cd server && npm test`. Next step is to continue the single-concern survey on `FileViewerTextEditor.ts` and the remaining under-ceiling coordinators, especially where request lifecycle or persistence code may still hide secondary subsystems.

**Iteration 31 (2026-03-11):** Stayed on `FileViewerTextEditor.ts` because it still mixed CodeMirror lifecycle with a separate text-file actions subsystem. Extracted save-file persistence, clipboard copy feedback, and text download side effects into `FileViewerTextActions.ts` (108 LOC); `FileViewerTextEditor.ts` now focuses on editor construction, dirty tracking, markdown edit-mode transitions, and annotation-aware selection updates instead of owning button-driven file actions inline. Verified with `npm run build` and `cd server && npm test`. Next step is to continue the single-concern survey on `FileViewerContentPresenter.ts`, `FileViewerModal.ts`, and the remaining sub-700 coordinators where media presentation or startup wiring may still hide distinct subsystems.

**Iteration 32 (2026-03-11):** Stayed on `FileViewerContentPresenter.ts` because it still mixed request ownership with the full file-loading and media/text presentation runtime. Extracted extension detection, raw asset URL building, file-content and annotation fetches, image/PDF rendering, and text/markdown presentation into `FileViewerContentLoader.ts` (257 LOC); `FileViewerContentPresenter.ts` dropped from 353 LOC to 180 LOC and now focuses on current file identity, abortable show-request coordination, and viewer-state resets instead of owning mixed presentation logic inline. Verified with `npm run build` and `cd server && npm test`. Next step is to continue the single-concern survey on `FileViewerModal.ts`, then check the remaining sub-700 coordinators for hidden media/startup subsystems or lingering mixed responsibilities.

**Iteration 33 (2026-03-11):** Stayed on `FileViewerModal.ts` because it still mixed modal shell coordination with a separate keyboard/navigation runtime. Extracted document-level Escape/Arrow handling, vim-style double-Escape behavior, deferred UI timers, and file-cycle state into `FileViewerModalRuntime.ts` (214 LOC); `FileViewerModal.ts` dropped from 496 LOC to 347 LOC and now focuses on modal DOM construction, shell actions, presenter wiring, and lifecycle coordination instead of owning input/runtime mechanics inline. Verified with `npm run build` and `cd server && npm test`. Next step is to continue the single-concern survey on the remaining sub-700 coordinators, starting with `main.ts`, `FileViewerContentPresenter.ts`, and any UI shells that still combine DOM chrome with independent runtime state machines.

**Iteration 34 (2026-03-11):** Stayed on `main.ts` because it still mixed application bootstrap with the map action command surface. Extracted worker focus, new-worker prompting, city pin/unpin/move websocket mutations, and dormant remote-city activation into `FrontendMapActions.ts` (96 LOC); `main.ts` dropped from 521 LOC to 447 LOC and now focuses on renderer/UI setup, state-sync reactions, initial focus, and lifecycle coordination instead of owning prompt/send/activate behavior inline. Verified with `npm run build` and `cd server && npm test`. Next step is to continue the single-concern survey on the remaining sub-700 coordinators, especially whether `FrontendStateSync.ts`, `CityHUD.ts`, or `FileViewerAnnotations.ts` still hide distinct runtime subsystems despite sitting under the size ceiling.

**Iteration 35 (2026-03-11):** Stayed on `CityHUD.ts` because it still mixed HUD shell coordination with the fiber/search content runtime. Extracted fiber loading/rendering, search input state, local fiber filtering, websocket search result handling, and handoff/open-file dispatch into `CityHUDContent.ts` (333 LOC); `CityHUD.ts` dropped from 721 LOC to 445 LOC and now focuses on modal visibility, tabs, header actions, worker chips, and file-tree delegation instead of owning searchable content state inline. Verified with `npm run build` and `cd server && npm test`. Next step is to continue the single-concern survey on `FrontendStateSync.ts` and `FileViewerAnnotations.ts`, then re-check whether any remaining under-ceiling coordinators still hide independent runtime subsystems.

**Iteration 36 (2026-03-11):** Stayed on `FrontendStateSync.ts` because it still mixed websocket/state synchronization with a separate activity-buffer runtime. Extracted session-activity keying, snapshot reconciliation, live activity buffering, and diagnostics stats into `FrontendActivityStore.ts` (110 LOC); `FrontendStateSync.ts` dropped from 332 LOC to 253 LOC and now focuses on socket lifecycle, message dispatch, and normalized state updates instead of owning activity bookkeeping inline. Verified with `npm run build` and `cd server && npm test`. Next step is to continue the single-concern survey on `FileViewerAnnotations.ts`, then re-check the remaining under-ceiling frontend/server coordinators for hidden runtime or persistence subsystems.

**Iteration 37 (2026-03-11):** Stayed on `main.ts` because it still mixed application bootstrap with the frontend runtime lifecycle: resize/render loop ownership, HUD rerender coalescing, mock-state fallback, and teardown. Extracted that state machine into `FrontendAppRuntime.ts` (149 LOC); `main.ts` dropped from 448 LOC to 367 LOC and now focuses on scene/UI wiring, state-sync reactions, initial focus, and map interaction coordination instead of owning runtime cleanup and frame scheduling inline. Verified with `npm run build` and `cd server && npm test`. Next step is to continue the single-concern survey on `FileViewerAnnotations.ts`, then re-check the remaining under-ceiling coordinators for hidden interaction or persistence subsystems.

**Iteration 38 (2026-03-11):** Stayed on `src/ui/FileViewerAnnotations.ts` because it still mixed annotation-panel coordination with the full text-selection annotation runtime. Extracted selection-range mapping, toolbar/input lifecycle, and text annotation persistence into `FileViewerTextAnnotations.ts` (267 LOC); `FileViewerAnnotations.ts` dropped from 601 LOC to 410 LOC and now focuses on panel state, highlight updates, worker handoff, file-as-fiber actions, and image/text delegate coordination instead of owning the text-selection UI inline. Verified with `npm run build` and `cd server && npm test`. Next step is to re-survey the remaining sub-700 coordinators, especially `FileViewerAnnotations.ts`, `FileViewerContentPresenter.ts`, and other frontend shells, for any last embedded runtime subsystems now that the file viewer annotation stack has been split cleanly.

**Iteration 39 (2026-03-11):** Stayed on `src/ui/FileViewerAnnotations.ts` because it still mixed annotation-panel coordination with a separate annotation-transport runtime. Extracted global-comment/button visibility wiring, worker loading/picker flow, send-to-worker dispatch, and file-as-fiber feedback into `FileViewerAnnotationTransport.ts` (160 LOC); `FileViewerAnnotations.ts` dropped from 410 LOC to 310 LOC and now focuses on annotation list state, highlight updates, preview rendering, guarded reloads, and image/text delegates instead of owning transport side effects inline. Verified with `npm run build` and `cd server && npm test`. Next step is to re-survey the remaining sub-700 coordinators, especially `FileViewerContentPresenter.ts`, `CityHUD.ts`, and other UI shells, for any last embedded runtime subsystems now that the file viewer annotation stack is split cleanly.

**Iteration 40 (2026-03-11):** Stayed on `src/ui/FileViewerAnnotations.ts` because it still mixed the top-level annotation coordinator with the annotation collection and panel lifecycle. Extracted preview rendering, panel refresh/goto behavior, guarded image-annotation persistence, and shared annotation list state into `FileViewerAnnotationPanelController.ts` (169 LOC); `FileViewerAnnotations.ts` dropped from 310 LOC to 208 LOC and now coordinates panel state, transport, text/image delegates, and CodeMirror highlight syncing instead of owning the panel runtime inline. Verified with `npm run build` and `cd server && npm test`. Next step is to re-survey the remaining sub-700 coordinators, especially `FileViewerContentPresenter.ts`, `CityHUD.ts`, and other UI shells, for any last embedded runtime subsystems now that the file viewer annotation stack has been split into narrow modules.

**Iteration 41 (2026-03-11):** Re-surveyed the remaining sub-700 coordinators and found that `server/src/HttpApi.ts` still mixed HTTP route dispatch with the hook/runtime sidecar: recent-files lookup, file-touch reconciliation, runtime diagnostics, and the hook-session mapping state machine. Extracted that subsystem into `HttpApiHooksRuntime.ts` (231 LOC); `HttpApi.ts` dropped from 682 LOC to 485 LOC and now focuses on endpoint routing plus the remaining activation/playground family instead of owning hook/runtime bookkeeping inline. Verified with `npm run build` and `cd server && npm test`. Next step is to re-survey the remaining server/frontend coordinators, especially whether `HttpApi.ts` should shed activation/playground next or whether a frontend shell now has the larger single-concern gap.

**Iteration 42 (2026-03-11):** Stayed on `server/src/HttpApi.ts` because it still mixed pure HTTP route dispatch with the remaining activation and playground endpoint families. Extracted dormant-city agent startup into `HttpApiActivation.ts` (78 LOC) and playground listing/HTML serving into `HttpApiPlayground.ts` (137 LOC); `HttpApi.ts` dropped from 485 LOC to 315 LOC and now focuses on endpoint routing plus shared JSON/request helpers instead of owning those endpoint runtimes inline. Verified with `npm run build` and `cd server && npm test`. Next step is to re-survey the remaining sub-700 coordinators, especially frontend shells like `FileViewerContentPresenter.ts` and `CityHUD.ts`, to confirm whether any still hide independent runtime or presentation subsystems despite sitting under the size ceiling.

**Iteration 43 (2026-03-11):** Re-surveyed the remaining frontend coordinators and found that `src/ui/TapestryView.ts` still mixed DAG/detail orchestration with a separate tapestry-data runtime: fetch request ownership, URL/hash synchronization, static export state, static file modal ownership, and artifact preloading. Extracted that subsystem into `TapestryViewRuntime.ts` (269 LOC); `TapestryView.ts` dropped from 670 LOC to 511 LOC and now focuses on graph/detail/sidebar/annotation coordination instead of owning data-state machinery inline. Verified with `npm run build` and `cd server && npm test`. Next step is to re-survey the remaining sub-700 coordinators, especially `CityHUD.ts` and other frontend shells, to confirm whether any still hide independent action or runtime subsystems despite sitting under the size ceiling.

**Iteration 44 (2026-03-11):** Re-surveyed the remaining sub-700 coordinators and found that `server/src/EventWatcher.ts` still mixed file watching with the session-activity retention/state machine: backfill parsing, recent-activity storage, working-timeout cleanup, and tracked-session pruning. Extracted that subsystem into `EventWatcherSessionState.ts` (281 LOC); `EventWatcher.ts` now focuses on events-file ownership, fs watch/poll lifecycle, and callback dispatch instead of owning activity/session bookkeeping inline. Verified with `cd server && npm test` and `npm run build`. Next step is to re-survey the remaining coordinators under the size ceiling, especially whether `CityHUD.ts`, `TapestryDagGraph.ts`, or another runtime shell still hides a distinct subsystem despite looking compact.

**Iteration 45 (2026-03-11):** Re-surveyed the remaining server coordinators and found that `server/src/KittyIntegration.ts` still mixed terminal coordination with a separate fiber-handoff runtime plus shared shell/path helpers used across the server. Extracted the delayed handoff/session bootstrapping flow into `KittyHandoff.ts` (131 LOC) and moved `shellEscape`/`expandHome` into `ShellPathUtils.ts` (19 LOC), updating the server modules and tests that depended on those helpers. `KittyIntegration.ts` dropped from 481 LOC to 339 LOC and now focuses on session focusing, worker creation, and worker termination instead of owning handoff orchestration and global shell utilities inline. Verified with `cd server && npm test` and `npm run build`. Next step is to re-survey the remaining under-ceiling coordinators, especially whether `KittyIntegration.ts`, `CityHUD.ts`, or `TapestryDagGraph.ts` still hide another distinct subsystem now that the handoff path has been separated.

**Iteration 46 (2026-03-11):** Re-surveyed the remaining server/frontend coordinators and found that `server/src/KittyIntegration.ts` still mixed terminal-worker orchestration with the separate tmux tab/session control path for focus and termination. Extracted that subsystem into `KittySessionController.ts` (123 LOC), including local/remote Kitty tab focus fallback, SSH-auth-forwarded remote attach commands, and worker kill routing, then added focused unit coverage in `KittySessionController.test.ts`. `KittyIntegration.ts` now focuses on worker creation plus handoff coordination instead of owning tab/session control inline. Verified with `cd server && npm test` and `npm run build`. Next step is to re-survey the remaining under-ceiling coordinators, especially `CityHUD.ts`, `TapestryDagGraph.ts`, and the surviving server shells, to decide whether the sweep is effectively complete or whether one last hidden subsystem remains.

**Iteration 47 (2026-03-11):** Re-surveyed the remaining under-ceiling coordinators and found that `src/ui/TapestryDagGraph.ts` still mixed SVG/runtime orchestration with the separate visibility state machine for section expansion, radial reveal/collapse animation, and node/link highlighting. Extracted that subsystem into `TapestryDagVisibility.ts` (405 LOC); `TapestryDagGraph.ts` dropped from 688 LOC to 373 LOC and now focuses on layout wiring, SVG construction, drag/zoom/tooltip behavior, and centering instead of owning visibility transitions inline. Verified with `npm run build` and `cd server && npm test`. Next step is to re-survey the remaining sub-700 coordinators, especially `CityHUD.ts` and the other frontend shells, to confirm whether any still hide a distinct runtime or whether the sweep is effectively complete.

**Iteration 48 (2026-03-11):** Re-surveyed the remaining frontend shells and found that `src/ui/CityHUD.ts` still mixed HUD shell coordination with the full header runtime: git-status summary rendering, claims/playground action buttons, worker-chip focus, and new-worker prompt/send flow. Extracted that subsystem into `CityHUDHeader.ts` (222 LOC); `CityHUD.ts` dropped from 445 LOC to 299 LOC and now focuses on modal visibility, tabs, content/file-tree delegation, and document-level close behavior instead of owning header actions inline. Verified with `npm run build` and `cd server && npm test`. Next step is to re-survey the remaining sub-700 coordinators, especially `TapestryView.ts`, `CityHUDContent.ts`, and the remaining frontend shells, to confirm whether any still hide a distinct runtime or whether the sweep is effectively complete.
